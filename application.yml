spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/family_agent
    username: user
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

qdrant:
  host: localhost
  port: 6333
  url: http://localhost:6333
  collection-name: expenses
